name: Glados

on:
  schedule:
    - cron: '24 0 * * *' # 每天早上 8 点 UTC 时间，可能需要根据你的时区调整
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    
    steps:
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Commit and push changes
        run: |
          python glados.py
